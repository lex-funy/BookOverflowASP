@model UserLoginModel
@{
    ViewData["Title"] = "Login";
}

<div class="container">
    <h2>@ViewData["Title"]</h2>
</div>

<section>
    <div class="container">
        <form method="post">
            <div class="form-group">
                <label asp-for="Email" class="control-label"></label>
                @* TODO: Remove value *@
                <input asp-for="Email" class="form-control" placeholder="Email" value="lexkrooswijk@live.nl"/>

                <span asp-validation-for="Email" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Password" class="control-label"></label>
                @* TODO: Remove value *@
                <input asp-for="Password" class="form-control" placeholder="Password" type="password" value="test"/>

                <span asp-validation-for="Password" class="text-danger"></span>
            </div>
            <div class="row sb">
                <input type="submit" class="btn" />
                <a href='javascript:history.go(-1)' class="btn">Back</a>
            </div>
        </form>
    </div>
</section>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
